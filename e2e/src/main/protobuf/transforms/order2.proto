syntax = "proto2";

package scalapb.transforms.order.order2;

import "scalapb/validate.proto";

option (scalapb.validate.package) = {
    field_transformations: [
        {
            when: {int32: {gt: 0}}
            set: {
                type: "ShouldNotCompile"
                scala_name: "bam"
            }
        }
    ]
};